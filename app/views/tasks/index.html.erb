<p style="color: green"><%= notice %></p>

<h1>Tasks</h1>

<div id="tasks">
  <% @tasks.each do |task| %>
    <p>
      <strong>Task:</strong>
      <%= link_to task.title, task %>
    </p>
    <%= render task %>
    <hr>
  <% end %>
</div>

<%= link_to "New task", new_task_path %>
